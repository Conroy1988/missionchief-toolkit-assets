name: Prepare Actions Security Commit v2

on:
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - ".github/workflows/prepare-actions-security-commit-v2.yml"
      - ".github/scripts/prepare_actions_security_commit.py"

permissions:
  contents: write

jobs:
  prepare:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out pull request branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Prepare immutable workflow blobs
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        run: python3 .github/scripts/prepare_actions_security_commit.py

      - name: Upload workflow blob map
        uses: actions/upload-artifact@v6
        with:
          name: prepared-actions-security-blobs
          path: |
            prepared-workflow-blobs.json
            changed-workflows.txt
          if-no-files-found: error
          retention-days: 1
