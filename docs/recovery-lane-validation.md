# Recovery lane validation

The recovery lane runs the canonical validator, JavaScript syntax check and source/distribution byte-parity check before any leased branch push. Normal pull-request validation remains mandatory after the push.
