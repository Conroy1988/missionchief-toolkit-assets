name: Stage v8 Godfather Builder

on:
  push:
    branches:
      - feat/v8-godfather-an-offer-he-cant-refuse
    paths:
      - ".github/workflows/inspect-v8-godfather-architecture.yml"

permissions:
  contents: read

jobs:
  staging:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Builder transport is being staged and is intentionally disabled."
